#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n = 0;
    int a[105];
    while (cin >> a[n++])
        ;

    sort(a, a + n);

    int k = 0;
    while (cout << a[n - (n - (k++))])

        return 0;
}